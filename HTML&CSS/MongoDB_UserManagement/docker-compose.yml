services:
  nginx:
    image: nginx:alpine
    ports:
      - "8082:80"
    volumes:
      - ./src:/var/www/html
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - php
    restart: unless-stopped
    develop:
      watch:
        - action: sync
          path: ./src
          target: /var/www/html

  php:
    extra_hosts:
      - "host.docker.internal:host-gateway"
    image: bitnami/php-fpm:latest
    volumes:
      - ./src:/var/www/html
    environment:
      - MONGO_HOST=host.docker.internal
      - MONGO_PORT=27017
      - MONGO_USER=
      - MONGO_PASS=
      - MONGO_DB=school
    restart: unless-stopped

  nodeapp:
    build: .
    container_name: my_mongo_node
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
    develop:
      watch:
        - action: rebuild
          path: package.json


